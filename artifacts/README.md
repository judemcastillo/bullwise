# Generated artifacts

Generated JSON is kept outside the repository root:

- `backtests/` contains downloaded histories, backtest reports, portfolio reports, and provider audits.
- `analysis/` contains research scans, datasets, training artifacts, diagnostics, and model reports.

The JSON files are intentionally ignored by Git. Keep checksum-frozen research artifacts unchanged and use a new versioned filename instead of replacing them.
